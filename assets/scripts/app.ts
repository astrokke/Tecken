import { startStimulusApp } from "@symfony/stimulus-bundle";
import "../styles/app.scss";
const app = startStimulusApp();
// le reste du code arrive en dessous, ne pas toucher les imports juste au dessus
